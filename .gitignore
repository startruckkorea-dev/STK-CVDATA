# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env

# Raw data (mirrored from SharePoint at build time, not committed)
raw_data/

# Built JSON. SharePoint is the only source the site reads, so these are
# staging for tools/publish_data.py — never deployed, never committed.
# This repo is public: committing them would publish the figures, and a copy
# in git would be a second source of truth that silently goes stale.
build/
docs/data/

# Secrets
.env.local
*.secrets
.msal_cache.json

# Editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Original snapshot (kept locally as reference, not committed)
code_snapshot.html
